%h1= @artist.name

%p
  %em= @artist.original_name

= raw @artist.description_html

%p
  Tags:
  - @artist.tag_list.each do |tag|
    = link_to tag, artist_tag_path(tag)

%h2 Song List

%table.table.table-striped
  %tr
    %th Name
    %th Release Date
  - @artist.songs.order(:name).each do |song|
    %tr
      %td= link_to song.name, song
      %td= song.released_at
